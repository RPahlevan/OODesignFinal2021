package common;

/**
 * Enum used to simplify event passing.
 */
public enum ProcedureOptions {
    FULL,
    SETUP,
    RELEASE,
    ACTIVATE,
    DEACTIVATE,
    RU,
    PARAM,
    ALARM,
    CARRIER,
    SCALING,
    POST,
    DIAGNOSTIC,
    RAT_TYPE;
}

